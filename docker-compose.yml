services:
  postgres:
    image: postgres:17
    container_name: kutuar-postgres

    environment:
      POSTGRES_DB: kutuar
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres1234567

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: