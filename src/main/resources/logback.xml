<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender para logar em arquivo, útil para ambientes de produção -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/synge.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- logs/synge.%d{yyyy-MM-dd}.log -->
            <fileNamePattern>logs/synge.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <!-- Manter no máximo 30 dias de logs -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Configuração para o nível de DEBUG ativo -->
    <root level="INFO"> <!-- Nível padrão para a aplicação -->
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

    <!-- Exemplo de configuração de nível para pacotes específicos -->
    <logger name="br.com.synge" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

    <!-- Para evitar que CPFs e senhas apareçam no log, é crucial que os DTOs e Models
         não tenham seus métodos toString() expondo esses dados.
         Além disso, ao logar, use placeholders e evite concatenar strings diretamente.
         Ex: logger.info("Usuário logado: {}", usuario.getUsername());
    -->

</configuration>
