DB_URL=jdbc:postgresql://localhost:5432/synge
DB_USER=postgres
DB_PASSWORD=postgres
JWT_SECRET=SyngeSistema2026SuperSecretoJWTChave256Bits123456789

# E-mail (recuperação de senha) — se deixar em branco, o sistema só loga o
# código no console em vez de enviar de verdade (modo desenvolvimento).
# Exemplo pronto pra usar com Gmail: crie uma "senha de app" em
# https://myaccount.google.com/apppasswords (não use a senha normal da conta).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=